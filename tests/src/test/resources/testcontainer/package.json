{
    "name": "hardhat-project",
    "version": "1.0.0",
    "description": "Ethereum development environment using Hardhat",
    "scripts": {
      "test": "npx hardhat test",
      "node": "npx hardhat node",
      "deploy": "npx hardhat run scripts/deploy.js --network localhost"
    },
    "dependencies": {
      "@nomicfoundation/hardhat-toolbox": "^3.0.0",
      "dotenv": "^16.0.3",
      "ethers": "^6.6.0",
      "hardhat": "^2.20.0"
    },
    "devDependencies": {
      "@types/node": "^20.9.0"
    }
}